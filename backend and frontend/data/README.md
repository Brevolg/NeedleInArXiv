# Data directory

Place the supplied files here using these names:

- `id_mapping_v1.parquet`
- `questions.parquet`
- optionally, a full corpus Parquet with `doc_id`, `title`, and `text`/`abstract`

Large data files are intentionally excluded from Git.

